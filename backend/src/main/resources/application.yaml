spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://127.0.0.1:54322/postgres}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:postgres}
  jpa:
    hibernate:
      #ddl-auto: none
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
server:
  port: 8080

supabase:
  url: ${SUPABASE_URL}
  anon-key: ${SUPABASE_ANON_KEY}
  service-role-key: ${SUPABASE_SERVICE_ROLE_KEY}
  jwt-secret: ${SUPABASE_JWT_SECRET}
  s3:
    endpoint: http://127.0.0.1:54321/storage/v1/s3
    region: local
    access-key: 625729a08b95bf1b7ff351a663f3a23c
    secret-key: 850181e4652dd023b7a98c58ae0d2d34bd487ee0cc3254aed6eda37307425907
    bucket: task-attachments
  storage:
    # URL for Frontend, for picture access
    public-url: http://127.0.0.1:54321/storage/v1/object/public/task-attachments/
